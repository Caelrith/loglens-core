[package]
name = "loglens-core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
logfmt = "0.0.2"
regex = "1.10"
chrono = { version = "0.4", features = ["serde"] }
humantime = "2.1"